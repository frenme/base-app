name: golang-prod

x-micro-service: &micro-service
  restart: always
  networks:
    - app-shared-network
  env_file:
    - .env

services:
  user-service:
    <<: *micro-service
    container_name: user-service-golang-prod
    build:
      context: .
      dockerfile: user-service/Dockerfile
    ports:
      - "${USER_SERVICE_PORT}:${USER_SERVICE_PORT}"

  order-service:
    <<: *micro-service
    container_name: order-service-golang-prod
    build:
      context: .
      dockerfile: order-service/Dockerfile
    ports:
      - "${ORDER_SERVICE_PORT}:${ORDER_SERVICE_PORT}"

  shared-service:
    <<: *micro-service
    container_name: shared-service-golang-prod
    build:
      context: .
      dockerfile: shared-service/Dockerfile
    ports:
      - "${SHARED_SERVICE_PORT}:${SHARED_SERVICE_PORT}"

networks:
  app-shared-network:
    external: true
