name: golang-dev

x-micro-service: &micro-service
  restart: unless-stopped
  env_file:
    - .env
  volumes:
    - .:/app
  logging:
    options:
      max-size: "10m"
      max-file: "3"

services:
  agency-service:
    <<: *micro-service
    image: agency-service:0.0.1-dev
    build:
      context: .
      dockerfile: agency-service/Dockerfile.dev
      args:
        GOLANG_VERSION: ${GOLANG_VERSION}
    networks:
      app_shared_network:
        aliases:
          - agency-service-golang
    expose:
      - "${AGENCY_SERVICE_PORT}"

  user-service:
    <<: *micro-service
    image: user-service:0.0.1-dev
    build:
      context: .
      dockerfile: user-service/Dockerfile.dev
      args:
        GOLANG_VERSION: ${GOLANG_VERSION}
    networks:
      app_shared_network:
        aliases:
          - user-service-golang
    expose:
      - "${USER_SERVICE_PORT}"

  temp-service:
    <<: *micro-service
    image: temp-service:0.0.1-dev
    build:
      context: .
      dockerfile: temp-service/Dockerfile.dev
      args:
        GOLANG_VERSION: ${GOLANG_VERSION}
    networks:
      app_shared_network:
        aliases:
          - temp-service-golang
    expose:
      - "${TEMP_SERVICE_PORT}"

  shared-service:
    <<: *micro-service
    image: shared-service:0.0.1-dev
    build:
      context: .
      dockerfile: shared-service/Dockerfile.dev
      args:
        GOLANG_VERSION: ${GOLANG_VERSION}
    networks:
      app_shared_network:
        aliases:
          - shared-service-golang

networks:
  app_shared_network:
    external: true